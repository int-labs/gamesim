// FinLit config + fit. NOT an engine — the server owns every number.
// Operator-hydrated catalogs, plus `vocFit` for the editor's live preview.
export * from './types';
export * from './fit';
export * from './engineMeta';
export * as finlitConfig from './config';
