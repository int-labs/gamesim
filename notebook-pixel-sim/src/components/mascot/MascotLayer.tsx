// The mascot used to float at the bottom-right. The current design moves Amelia
// into the top-right of the HUD via <HUDMascot/>. This component is kept as a
// no-op so existing imports don't break — but it renders nothing.
export function MascotLayer() {
  return null;
}
